export enum EStatus {
  Active = 0,
  Unauthenticated = 1,
  Disabled = 2,
}

export enum EUserRole {
  SuperAdmin = 0,
  Admin = 1,
  Member = 2,
  User = 3,
  Public = 4,
}

export enum EPageType {
  SinglePage = 0,
  PageList = 1,
  ResList = 2,
  System = 3,
}
