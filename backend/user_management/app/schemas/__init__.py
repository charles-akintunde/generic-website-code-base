"""
Initialization module for the schema package.

This module imports the User, Team, and Application schema
so they can be easily accessed from the schema package.

Attributes:
    User (class): The User schema class.
    Team (class): The Team schema class.
    Application (class): The Application schema class.
"""

from .user import UserCreate

