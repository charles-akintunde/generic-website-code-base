# from sqlalchemy import Table, Column, ForeignKey
# from sqlalchemy.dialects.postgresql import UUID
# from . import Base 

# T_UsersPageContents = Table(
#     'T_UsersPageContents',
#     Base.metadata,
#     Column('UI_ID', UUID(as_uuid=True), ForeignKey('T_UserInfo.UI_ID'), primary_key=True),
#     Column('PC_ID', UUID(as_uuid=True), ForeignKey('T_PageContent.PC_ID'), primary_key=True)
# )
